		<<case Remy>>
			雷米
			<<if $livestockEscape isnot undefined>>
					<span class="red">想要把你带回农场。</span>
			<<elseif $remyromance is 1>>
				<<if $NPCName[_i].dom gte 80>>
					<<if $NPCName[_i].love gte 60>>
						<span class="red">想要奖励你吃你最喜欢的苹果。</span>
					<<else>>
						<span class="red">把你视为调教的最成功的小奶牛。</span>
					<</if>>
				<<elseif $NPCName[_i].dom gte 40>>
					<<if $NPCName[_i].love gte 60>>
						认为你是一头<span class="green">可爱的小牛。</span>
					<<else>>
						<span class="lewd">认为你很听话很可爱。</span>
					<</if>>
				<<elseif $NPCName[_i].dom gte 20>>
					<span class="lewd">很喜欢摸你的头发。</span>
				<<else>>
					<span class="lewd">认为你不只是一头奶牛。</span>
			   	 <</if>>
			<<elseif $remysex is 2 and $NPCName[_i].lust gte 70>>
				<<if $NPCName[_i].dom gte 60>>
					认为你是好用的<span class="lewd">玩具。</span>
				<<else>>
					想要你<span class="lewd">更多的牛奶。</span>
				<</if>>
			<<else>>
				<<if $livestock_intro isnot undefined>>
					<<if $NPCName[_i].love gte 50>>
						<<if $NPCName[_i].dom gte 25>>
							认为你是<span class="red">牧群中的骄傲</span>。
						<<else>>
							希望你不再这么叛逆。
						<</if>>
					<<elseif $NPCName[_i].love gte 25>>
						<<if $NPCName[_i].dom gte 25>>
							<span class="pink">对你的表现感到满意</span>。
						<<else>>
							想要<span class="purple">让你认清自己的位置</span>。
						<</if>>
					<<elseif $NPCName[_i].love lte $npclovelow>>
						<<if $NPCName[_i].dom gte $npcdomhigh>>
							想<span class="red">榨干你所有的乳汁</span>。
						<<elseif $NPCName[_i].dom lte $npcdomlow>>
							<span class="red">决心要击垮你</span>。
						<<else>>
							认为你是<span class="red">一头倔强的<<= $player.gender_appearance is "m" ? "公牛" : "母牛">></span>。
						<</if>>
					<<else>>
						<<if $NPCName[_i].dom gte 25>>
							正<span class="purple">致力于提高你的产量</span>。
						<<else>>
							认为你<span class="red">需要被彻底征服</span>。
						<</if>>
					<</if>>
				<<else>>
					对你没什么看法。
				<</if>>
			<</if>>
		<!-- Default cases -->