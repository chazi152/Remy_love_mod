	<<case 8>>
		<<if $livestockEscape isnot undefined and random(1, 2) is 2>>
			你的思绪飘向了雷米和那座地下农场，
			<<if $syndromeremy is 1>>
				你感觉有些怀念。
				<<ltrauma>><<lstress>><<trauma -6>><<stress -6>>
			<<else>>
				<<nnpc_he "Remy">>和<<nnpc_he "Remy">>的手下们或许正在<span class="red">寻找你</span>。
				<<gtrauma>><<gstress>><<trauma 6>><<stress 6>>
			<</if>>
		<<else>>
			你的脑海里出现了贝利的身影，
			<<if $renttime lte 0>>
				<<babyRent true>>
				<<if $money gte $rentmoney + ($babyRent or 0)>>
					你有足够的钱支付给<<nnpc_him "Bailey">>。
					<<if $baileydefeatedchain gte 1>>
						但你想知道你是否真的需要这样做。
					<<else>>
						真是万幸。
					<</if>>
					<<ltrauma>><<lstress>><<trauma -6>><<stress -6>>
				<<else>>
					你没有足够的钱支付给<<nnpc_him "Bailey">>。
					<<if $baileydefeatedchain gte 1>>
						你想知道你是否需要这样做。
					<<else>>
						你想知道是否有什么方法可以快速赚钱。
					<</if>>
					<<gtrauma>><<gstress>><<trauma 6>><<stress 6>>
				<</if>>
			<<else>>
				<<babyRent true>>
				<<if $money gte $rentmoney + ($babyRent or 0)>>
					你有足够的钱在<<rentday>>支付给<<nnpc_him "Bailey">>，
					<<if $baileydefeatedchain gte 1>>
						但你想知道你是否真的需要这样做。
					<<else>>
						真是万幸。
					<</if>>
					<<ltrauma>><<lstress>><<trauma -6>><<stress -6>>
				<<else>>
					你没有足够的钱在<<rentday>>支付给<<nnpc_him "Bailey">>。
					<<if $baileydefeatedchain gte 1>>
						你想知道你是否需要这样做。
					<<else>>
						你想知道是否有什么方法可以快速赚钱。
					<</if>>
					<<gtrauma>><<gstress>><<trauma 6>><<stress 6>>
				<</if>>
			<</if>>
		<</if>>
	<<case 7>>